-- AI Sales Assistant CRM - Database Schema Reference
-- PostgreSQL / Supabase

-- Run migrations via Alembic:
-- cd backend && alembic upgrade head

-- Tables: users, companies, contacts, lead_scores, buying_signals,
-- emails, email_events, meetings, call_logs, notifications, audit_logs
